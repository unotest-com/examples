# Viewer UI suite. Not part of `dogfood` — these need a viewer running over
# the frozen fixture project; scripts/dogfood-viewer.mjs starts it and
# passes its URL in as APP_BASE_URL.
scenarios:
  - viewer/scenario-tree
  - viewer/overview
  - viewer/run-history
  - viewer/run-from-ui
