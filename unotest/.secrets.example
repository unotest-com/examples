# Copy to unotest/.secrets (postinstall does it for you).
# The playground's own demo login, public and hardcoded in the page.
# Kept in a secrets file on purpose: it exercises the masking.
LOGIN=admin
PASSWORD=playground

# Only our own box scenarios need this one, and only we have it.
BOX_RO_PASSWORD=
