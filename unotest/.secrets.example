# Copy to unotest/.secrets (postinstall does it for you).
# Demo credentials for the playground's fake login (/dashboard).
# They are PUBLIC (hardcoded in the playground) — kept here as secrets
# deliberately, so every dogfood run exercises the masking machinery.
LOGIN=admin
PASSWORD=playground
