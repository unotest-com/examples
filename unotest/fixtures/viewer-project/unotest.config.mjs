// Frozen consumer project the viewer e2e scenarios run against. No
// baseUrl: both scenarios drive about:blank, so the viewer (and the Run
// button in its UI) work with no network at all.
/** @type {Partial<import('@unotest/web').UnotestConfig>} */
export default {};
