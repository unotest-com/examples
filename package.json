{
  "name": "unotest-examples",
  "version": "0.30.0",
  "private": true,
  "description": "The real end-to-end suite unotest runs against playground.unotest.com — copy it as a starting point for your own tests.",
  "license": "MIT",
  "author": "Ivan Volkov <ivan@volkov.io>",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "setup": "npm --prefix unotest run setup",
    "seed": "node unotest/fixtures/db/seed.mjs",
    "lint": "npm --prefix unotest exec unotest-web -- lint",
    "smoke": "npm --prefix unotest exec unotest-web -- collection dogfood-smoke",
    "dogfood": "npm --prefix unotest exec unotest-web -- collection dogfood",
    "viewer": "npm --prefix unotest exec unotest-web -- viewer"
  }
}
